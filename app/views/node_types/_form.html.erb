<%= form_for(@node_type) do |f| %>
  <% if @node_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node_type.errors.count, "error") %> prohibited this node_type from being saved:</h2>

      <ul>
      <% @node_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :reachable_img %><br />
    <%= f.text_field :reachable_img %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
